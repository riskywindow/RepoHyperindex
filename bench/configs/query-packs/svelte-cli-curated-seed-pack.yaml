schema_version: "1"
query_pack_id: svelte-cli-curated-seed-pack
corpus_id: svelte-cli
description: Manual seed query pack template for the selected small real corpus.
queries:
  - query_id: svelte-cli-exact-seed
    type: exact
    title: Manual exact seed for svelte-cli
    text: TODO_REPLACE_EXACT_TOKEN_AFTER_BOOTSTRAP
    tags:
      - real-corpus
      - manual
      - seed
      - svelte-cli
      - exact
    notes: Replace with a stable exact token after local bootstrap and snapshot review.
    limit: 10
  - query_id: svelte-cli-symbol-seed
    type: symbol
    title: Manual symbol seed for svelte-cli
    symbol: TODO_REPLACE_SYMBOL_AFTER_BOOTSTRAP
    scope: repo
    tags:
      - real-corpus
      - manual
      - seed
      - svelte-cli
      - symbol
    notes: Replace with a stable exported symbol after local bootstrap and curation.
    limit: 10
  - query_id: svelte-cli-semantic-seed
    type: semantic
    title: Manual semantic seed for svelte-cli
    text: TODO replace with a stable semantic question after local bootstrap
    path_globs:
      - "**/*"
    rerank_mode: hybrid
    tags:
      - real-corpus
      - manual
      - seed
      - svelte-cli
      - semantic
    notes: Replace with a repo-specific semantic question once the local snapshot is pinned.
    limit: 10
  - query_id: svelte-cli-impact-seed
    type: impact
    title: Manual impact seed for svelte-cli
    target_type: file
    target: __manual_verification_required__
    change_hint: modify_behavior
    tags:
      - real-corpus
      - manual
      - seed
      - svelte-cli
      - impact
    notes: Replace with a stable impact anchor after local bootstrap and manual review.
    limit: 10
